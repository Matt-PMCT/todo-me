security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        # API firewall - stateless authentication using API tokens
        api:
            pattern: ^/api/
            stateless: true
            provider: app_user_provider
            custom_authenticators:
                - App\Security\ApiTokenAuthenticator

        # Web firewall - session-based authentication with form login
        main:
            lazy: true
            provider: app_user_provider

            # Form login configuration
            form_login:
                login_path: app_login
                check_path: app_login
                default_target_path: app_task_list
                username_parameter: _username
                password_parameter: _password
                enable_csrf: true

            # Logout configuration
            logout:
                path: app_logout
                target: app_login

            # Remember me functionality
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800  # 1 week
                path: /

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # Public auth endpoints - no authentication required (API)
        - { path: ^/api/v1/auth/register$, roles: PUBLIC_ACCESS }
        - { path: ^/api/v1/auth/token$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/v1/auth/refresh$, roles: PUBLIC_ACCESS, methods: [POST] }

        # All other API endpoints require authentication
        - { path: ^/api/, roles: ROLE_USER }

        # Public web pages - login and register
        - { path: ^/login$, roles: PUBLIC_ACCESS }
        - { path: ^/register$, roles: PUBLIC_ACCESS }

        # Protected web pages - task management requires login
        - { path: ^/tasks, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
