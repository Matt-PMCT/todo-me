<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #4f46e5;
            color: white;
            padding: 20px;
            border-radius: 8px 8px 0 0;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
        }
        .content {
            background-color: #f9fafb;
            padding: 30px;
            border: 1px solid #e5e7eb;
            border-top: none;
            border-radius: 0 0 8px 8px;
        }
        .notification-type {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 9999px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            margin-bottom: 16px;
        }
        .type-task_due_soon {
            background-color: #fef3c7;
            color: #92400e;
        }
        .type-task_overdue {
            background-color: #fee2e2;
            color: #991b1b;
        }
        .type-task_due_today {
            background-color: #dbeafe;
            color: #1e40af;
        }
        .type-recurring_created {
            background-color: #d1fae5;
            color: #065f46;
        }
        .type-system {
            background-color: #e5e7eb;
            color: #374151;
        }
        .message {
            font-size: 16px;
            margin: 16px 0;
        }
        .task-card {
            background-color: white;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 16px;
            margin: 16px 0;
        }
        .task-title {
            font-weight: 600;
            font-size: 18px;
            color: #111827;
            margin-bottom: 8px;
        }
        .task-meta {
            color: #6b7280;
            font-size: 14px;
        }
        .btn {
            display: inline-block;
            background-color: #4f46e5;
            color: white;
            padding: 12px 24px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            margin-top: 16px;
        }
        .btn:hover {
            background-color: #4338ca;
        }
        .footer {
            text-align: center;
            color: #9ca3af;
            font-size: 12px;
            margin-top: 32px;
            padding-top: 16px;
            border-top: 1px solid #e5e7eb;
        }
        .footer a {
            color: #6b7280;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Todo App</h1>
    </div>
    <div class="content">
        <span class="notification-type type-{{ type }}">
            {% if type == 'task_due_soon' %}
                Due Soon
            {% elseif type == 'task_overdue' %}
                Overdue
            {% elseif type == 'task_due_today' %}
                Due Today
            {% elseif type == 'recurring_created' %}
                New Task
            {% else %}
                Notification
            {% endif %}
        </span>

        <h2>{{ title }}</h2>

        {% if message %}
            <p class="message">{{ message }}</p>
        {% endif %}

        {% if data.taskTitle is defined %}
            <div class="task-card">
                <div class="task-title">{{ data.taskTitle }}</div>
                {% if data.dueDate is defined %}
                    <div class="task-meta">Due: {{ data.dueDate }}</div>
                {% endif %}
            </div>
        {% endif %}

        {% if data.taskId is defined %}
            <a href="{{ appUrl }}/tasks#task-{{ data.taskId }}" class="btn">View Task</a>
        {% endif %}
    </div>

    <div class="footer">
        <p>You received this email because you have notifications enabled for your Todo App account.</p>
        <p>
            <a href="{{ appUrl }}/settings/notifications">Manage notification preferences</a>
        </p>
    </div>
</body>
</html>
